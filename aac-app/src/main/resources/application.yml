spring:
  main:
    allow-circular-references: true
  application:
    name: dows-aac
    appId: 1
  profiles:
    include: |
      mysql,apidoc,aac,captcha,cache,rade,server,open,feign
    active: dev

  web:
    resources:
      static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/



